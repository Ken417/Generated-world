using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DelaunayDiagramMeshGenerator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //Mesh Create(int resolution,int pointNum)
    //{
    //    Vector3[] vertices = new Vector3[pointNum];

    //    Vector3 vec = Vector3.zero;
    //    for(int i =0; i < pointNum; i++)
    //    {
    //        vec.x = Random.Range(0, resolution);
    //        vec.z = Random.Range(0, resolution);
    //        vertices[i]=vec;
    //    }
    //    for (int i = 0; i < pointNum; i++)
    //    {
    //        for (int i2 = 0; i2 < pointNum; i2++)
    //        {
    //            x1 = []
    //        }
    //    }
    //}
}
